# Copy this file to .env and fill in your values.
#
# You need at minimum:
#   1. An LLM source          — GITHUB_PAT (Copilot)  OR  LLM_API_KEY (OpenAI-compatible)
#   2. A transcription source — WHISPER_API_KEY + WHISPER_BASE_URL
#
# Usage:
#   cp .env.example .env
#   # edit .env with your values
#   docker compose up -d

# =====================================
# --- GitHub Copilot (LLM via fork) ---
# =====================================

# Personal access token with the `copilot` scope — the fork's primary LLM source.
# Does not require a separate OpenAI account or billing.
# Generate at: https://github.com/settings/tokens
# GITHUB_PAT=github_pat_...

# The Copilot model to use. Required when using GITHUB_PAT.
# Examples: gpt-4o, claude-sonnet-4.5, o1-mini
# Run 'Fetch Models' in the UI after entering your PAT to see all available models.
# GITHUB_MODEL=gpt-4o

# ========================================
# --- LLM (OpenAI-compatible or proxy) ---
# ========================================

# Use instead of (or alongside) GITHUB_PAT.
# Point OPENAI_BASE_URL at a local or compatible server (e.g. Ollama, LiteLLM)
# to use self-hosted models.
# LLM_API_KEY=sk-your-openai-or-compatible-key
# LLM_MODEL=gpt-4o
# OPENAI_BASE_URL=http://localhost:11434/v1

# ================================
# --- Whisper (transcription)  ---
# ================================

# Podly uses an OpenAI-compatible transcription API.
# Set WHISPER_API_KEY and WHISPER_BASE_URL for your provider.
#
# Example — Groq (free tier, fast):
#   WHISPER_API_KEY=gsk_...
#   WHISPER_BASE_URL=https://api.groq.com/openai/v1
#   WHISPER_MODEL=whisper-large-v3-turbo
#
# Example — OpenAI:
#   WHISPER_API_KEY=sk-...
#   WHISPER_BASE_URL=https://api.openai.com/v1
#   WHISPER_MODEL=whisper-1

WHISPER_API_KEY=
WHISPER_BASE_URL=https://api.openai.com/v1
WHISPER_MODEL=whisper-1
# WHISPER_TIMEOUT_SEC=600
# WHISPER_CHUNKSIZE_MB=24

# =====================
# --- Authentication ---
# =====================

# Set REQUIRE_AUTH=true to enable login for the web UI, feeds, and downloads.
# REQUIRE_AUTH=true
# PODLY_ADMIN_USERNAME=podly_admin
# PODLY_ADMIN_PASSWORD=ChangeMe123!
# PODLY_SECRET_KEY=replace-with-a-strong-random-value
